# Task 2.3: Accessibility Audit Report.
Accessibility Audit for my `index.html` page
### Issues found
1. The profile image has generic alternative text("placeholder profile image"), which WAVE flagged as suspicious.
2. The page did not include semantic page regions such as `<header>`, `<main>` and `<footer>`.
3. We have an <hr> tag for line which is unnecessary.
### How you fixed them
1. Updated the image alt to more descriptive value: "profile picture of Grace-Loko".
2. Added semantic landmarks(`<header>`,`<main>` and `<footer>` to improve page navigation for assitive techologies.
3. Removed th <hr> tag.
### Final Lighthouse accessibility score
- Lighhouse accessbility score is `97%`
